#include "main.h"
#include <string.h>
/**
 * _puts - return the value of the lens of string
 * @str: the string
 * Return: Always 0
 */
void _puts(char *str)
{
	int puts(const char *str);

	puts(str);
}
