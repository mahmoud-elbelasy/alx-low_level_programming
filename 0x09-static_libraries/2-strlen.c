#include "main.h"
#include <string.h>
/**
 * _strlen - return the value of the lens of string
 * @s: the string
 * Return: the value of the len of the string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
